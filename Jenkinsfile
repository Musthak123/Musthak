pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Hello from GitHub'
            }
        }
        stage('Build') {
            steps {
                echo "Build is successful"
            }
        }
    }
}
